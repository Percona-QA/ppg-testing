---
# This verify playbook is not used.
#
# The pg_tde/tde molecule configuration uses the Ansible verifier but has no
# verify step in the test sequence. All tests (make installcheck USE_PGXS=1)
# run during the converge phase via tasks/redhat_tasks.yml and
# tasks/debian_tasks.yml.
#
# This file previously used HashiCorp Vault (now superseded by OpenBao/bao)
# and is retained for historical reference only.
- hosts: all
  tasks: []
